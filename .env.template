# ~/.claude/.env.template — Required environment variables
# Copy to ~/.config/last30days/.env and fill in values
# NEVER commit the actual .env file

# === Required for /last30days skill ===
OPENAI_API_KEY=           # OpenAI Responses API → Reddit search
XAI_API_KEY=              # xAI API → X/Twitter search

# === Optional — Enhanced functionality ===
GITHUB_TOKEN=             # Higher GitHub API rate limits (30 searches/min vs 10)
BLUESKY_HANDLE=           # e.g. user.bsky.social
BLUESKY_APP_PASSWORD=     # Settings → App Passwords → generate
SO_API_KEY=               # Stack Exchange API (10K req/day vs 300)
YOUTUBE_API_KEY=          # YouTube Data API v3 (console.cloud.google.com)

# === GitHub Actions (set as repo secrets, NOT in this file) ===
# ANTHROPIC_API_KEY    → gh secret set ANTHROPIC_API_KEY --repo TokyoFloripa/uptodate
# PERSONAL_ACCESS_TOKEN → gh secret set PERSONAL_ACCESS_TOKEN --repo TokyoFloripa/uptodate

# === Verification ===
# Run: python3 ~/.claude/skills/last30days/scripts/last30days.py --diagnose
# This shows which sources are available based on your current env vars.
